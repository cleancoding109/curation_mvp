# Integration Test Job
resources:
  jobs:
    integration_test_job:
      name: curation_framework_integration_test

      tasks:
        - task_key: run_integration_tests
          notebook_task:
            notebook_path: ../tests/integration_test_pipeline.py
            source: WORKSPACE

