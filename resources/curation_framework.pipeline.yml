# The main pipeline for curation_framework
resources:
  pipelines:
    curation_framework_pipeline:
      name: curation_framework_pipeline
      catalog: workspace
      schema: curation_framework_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
